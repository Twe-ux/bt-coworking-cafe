# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/your-database

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Stripe Configuration (for payments)
# Get your keys from: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key
# Webhook secret from: https://dashboard.stripe.com/test/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret
# Optional: Set to 'true' in production to use live mode
STRIPE_LIVE_MODE=false

# Development Configuration
# Set to 'true' in local dev environments without internet access
# This disables Google Fonts optimization during build
# Leave unset or set to 'false' in production
DISABLE_FONT_OPTIMIZATION=false

# Resend Email Service
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your-resend-api-key

# Email senders - Configure different senders for different email types
# Note: You must verify your domain in Resend dashboard first!
# If not specified, RESEND_FROM_EMAIL will be used as fallback

# Default sender (general emails)
RESEND_FROM_DEFAULT=CoworKing Café <noreply@coworkingcafe.fr>
# Or keep backward compatibility:
RESEND_FROM_EMAIL=CoworKing Café <noreply@coworkingcafe.fr>

# Booking/Reservation emails
RESEND_FROM_BOOKING=Réservations - CoworKing Café <reservations@coworkingcafe.fr>

# Contact form emails
RESEND_FROM_CONTACT=Contact - CoworKing Café <contact@coworkingcafe.fr>

# Cron Job Security
# Generate with: openssl rand -base64 32
# This secret is used to authenticate cron job requests
CRON_SECRET=your-cron-secret-here
